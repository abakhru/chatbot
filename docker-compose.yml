version: '3'

services:
  chatbot:
    env_file:
      - chatbot.env
    build: ./chatbot/
    command: /root/.poetry/bin/poetry run python chatbot/bot.py
    volumes:
      - ./chatbot:/app/chatbot
    ports:
      - "5000:5000"
    networks:
      - chatbot

networks:
  chatbot:
